# TODO: didnt work as a global mixing, was specified in admin of booking
class DynamicArrayMixin:
    class Media:
        js = ("booking/project.js",)
        css = {"all": ("booking/project.css",)}
